<h1>Artists Page</h1>
{{log 'Model is:' model}}
<table class="table table-bordered table-striped table-hover">
    <thead>
    <tr>
        <th>№</th>
        <th>Name</th>
        <th>iTunes_Id</th>
    </tr>
    </thead>
    <tbody>
    {{#each model as |artist index|}}
        <tr {{action 'findAlbums' artist.id}}>
            <th>{{index}}</th>
            <td>{{artist.name}}</td>
            <td>{{artist.itunes_id}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>