import { consecutiveNumber } from 'tunes-client/helpers/consecutive-number';
import { module, test } from 'qunit';

module('Unit | Helper | consecutive number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = consecutiveNumber([42]);
  assert.ok(result);
});
